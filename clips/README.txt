This folder is mandatory!!!
well you can create another one, but if you change the folder in "config.json" and it is not created, the script will not work.
so when you change the folder, create the folder manually.